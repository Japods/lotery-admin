const SET_REWARD_INFO = (state, reward) => {
  state.reward_info = reward;
};

const SET_IDENTIFICATION = (state, reward) => {
  state.identification = reward;
};

export default {
  SET_REWARD_INFO,
  SET_IDENTIFICATION,
};
