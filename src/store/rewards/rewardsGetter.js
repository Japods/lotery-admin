const GET_REWARD_INFO = (state) => {
  return state.reward_info;
};

const GET_IDENTIFICATION = (state) => {
  return state.identification;
};

export default {
  GET_IDENTIFICATION,
  GET_REWARD_INFO,
};
